services:
  web01:
  imagine: nginx:latest
  container_name: web01
  ports:
    - "4000:80"
  volumes:
    - ./web01:/usr/share/nginx/html:ro

  web02:
  imagine: nginx:latest
  container_name: web02
  ports:
    - "4001:80"
  volumes:
    - ./web02:/usr/share/nginx/html:ro
